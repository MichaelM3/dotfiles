interface:
  display_name: Generate Commits
  short_description: Create focused Conventional Commits
  default_prompt: Use $generate-commits to organize this branch's changes into focused
    Conventional Commits.
policy:
  allow_implicit_invocation: false
